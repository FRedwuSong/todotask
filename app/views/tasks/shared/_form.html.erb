<%= form_with model: task do |form| %>
<% if task.errors.any? %>
    <div>
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this content from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :title %><br />
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content %><br />
    <%= form.text_field :content %>
  </div>

  <div><%= form.submit %></div>
<% end %>
